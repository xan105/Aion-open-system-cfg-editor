"use strict";
exports.__esModule = true;
var State = (function () {
    function State(enabled, previousIds, childToParent, nextId, currentId, parentId) {
        if (enabled === void 0) { enabled = true; }
        if (previousIds === void 0) { previousIds = []; }
        if (childToParent === void 0) { childToParent = new Map(); }
        if (nextId === void 0) { nextId = 0; }
        if (currentId === void 0) { currentId = 0; }
        if (parentId === void 0) { parentId = 0; }
        this.enabled = enabled;
        this.previousIds = previousIds;
        this.childToParent = childToParent;
        this.nextId = nextId;
        this.currentId = currentId;
        this.parentId = parentId;
    }
    State.prototype.getNextId = function () {
        return (this.nextId += 1);
    };
    return State;
}());
exports.State = State;
//# sourceMappingURL=State.js.map